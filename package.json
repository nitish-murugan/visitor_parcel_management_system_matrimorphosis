{
  "name": "visitor-parcel-management-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix backend && npm run build --prefix frontend"
  }
}
